# Optional extras. The core (retrieval, sandbox, evals with MockClient) runs on
# the Python standard library alone — no install needed.
#
# For the real answer layer:
anthropic>=0.40
# For the HTTP API:
fastapi>=0.110
uvicorn>=0.29
# For tests (httpx backs FastAPI's TestClient):
pytest>=8.0
httpx>=0.27
# Lint:
ruff>=0.6
